# Templates

Runtime configuration is rendered by `lib/xray.sh`, `lib/hysteria2.sh`, `lib/output.sh`, and `lib/service.sh`.

This directory is reserved for future static templates that do not contain secrets.
